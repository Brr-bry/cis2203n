The Java String methods I used for the validation logic are .toString(), .equals(), .length(), .substring(). I used .toString() to format the user inputs into a string. Then I used .substring() and .length() to get the last 2 digits of the ID to form the correct password. Lastly, I used .equals() to compare the correct password from the input password.

